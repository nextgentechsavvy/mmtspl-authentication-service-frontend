import { LoginUserAuthMaster } from './login-user-auth-master';

describe('LoginUserAuthMaster', () => {
  it('should create an instance', () => {
    expect(new LoginUserAuthMaster()).toBeTruthy();
  });
});
