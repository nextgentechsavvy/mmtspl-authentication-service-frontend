import { EmployeeCompleteDetails } from './employee-complete-details';

describe('EmployeeCompleteDetails', () => {
  it('should create an instance', () => {
    expect(new EmployeeCompleteDetails()).toBeTruthy();
  });
});
