
use below commands to start the application...

> npm install     (It will install all the dependencies in " node_modules" folder)
> ng serve        (It will install related configaration in ".angular" folder)

		
		*************************  mmtspl-employee-service-frontend : port 5000  *******************************

		*************************  Backend  mmtspl-employee-service : port 9000  *******************************
	  
		Changes in	angular.json --> 
				 "serve": {
				          "builder": "@angular-devkit/build-angular:dev-server",
				          "options": {
				            "port": 5000
				          }, 
	
	URL  : http://localhost:5000/
	
	